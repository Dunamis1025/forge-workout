import React from 'react';
import ReactDOM from 'react-dom/client';
import WorkoutTracker from './forge-workout';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<WorkoutTracker />);
